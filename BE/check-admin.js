const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const admins = await prisma.user.findMany({ where: { role: 'ADMIN' }, select: { email: true, kota: true } }); console.log('Admins:', admins); const reports = await prisma.laporan.findMany({ select: { id: true, kota: true } }); console.log('Reports Kota Summary:'); const summary = {}; reports.forEach(r => { summary[r.kota] = (summary[r.kota] || 0) + 1; }); console.log(summary); } main().finally(() => prisma.$disconnect());
